# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'ios-flutter-frame' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-flutter-frame
  # /Users/fb/Documents/xituan/ios-flutter-frame/flutter-demo
  flutter_application_path = '/Users/fb/Documents/xituan/ios-flutter-frame/ft-youxuan'
  load File.join(flutter_application_path, '.ios', 'Flutter', 'podhelper.rb')
  
  # Pods for flutter-frame
  install_all_flutter_pods(flutter_application_path)
end
